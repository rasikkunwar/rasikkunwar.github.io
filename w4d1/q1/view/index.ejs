<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Add Cookie</title>
</head>

<body>
    <div class="container">
        <div id="addCookieForm">
            <fieldset>
                <legend>Add Cookie</legend>
                <form method="post" action="/addCookie">
                    <label for="key">Key</label>
                    <input type="text" name="key">
                    <label for="value">Value</label>
                    <input type="text" name="value">
                    <input type="submit" value="Add Cookie">
                </form>
            </fieldset>
        </div>
        <div id="cookies">
            <fieldset>
                <legend>List of the cookies</legend>
            <table>
                <tr>
                    <th>Key</th>
                    <th>Value</th>
                </tr>
                 <% if(cookies){
                for (key in cookies) { %>
                    <tr>
                    <td><%=key%></td>
                    <td><%=cookies[key]%></td>
                    </tr>
                <% }
                }
                else{ %>
                    <tr>
                        <td> No cookie found</td>
                    </tr>
                <%}
                %>
                
            </table>
            </fieldset>
        </div>
    </div>
</body>

</html>